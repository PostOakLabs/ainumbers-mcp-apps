{
  "tool_id": "mcp-tool-poisoning-scanner",
  "version": "1.1.0",
  "title": "MCP Tool-Poisoning & Prompt-Injection Manifest Scanner",
  "description": "Scans an MCP tool description or manifest for tool-poisoning and prompt-injection smells — instruction overrides, hidden/zero-width unicode, role-play framing, embedded model-directed instructions, and exfiltration hints. Heuristic pattern matcher mapped to OWASP ASI01 (Agent Goal Hijack); flags are signals, not proof.",
  "category": "cat-1",
  "tags": [
    "MCP",
    "tool poisoning",
    "prompt injection",
    "OWASP ASI01",
    "security",
    "manifest"
  ],
  "audience": [
    "engineer"
  ],
  "input_schema": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      }
    },
    "required": [
      "text"
    ]
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "risk": {
        "type": "number"
      },
      "findings": {
        "type": "array"
      }
    }
  },
  "mcp_tool_definition": {
    "name": "scan_tool_poisoning",
    "description": "Scan an MCP tool description/manifest for tool-poisoning and prompt-injection smells; returns a risk score and flagged patterns.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "text": {
          "type": "string",
          "description": "The tool description or manifest text to scan"
        }
      },
      "required": [
        "text"
      ]
    }
  },
  "execution": {
    "type": "browser-javascript",
    "entry": "tools/282-mcp-tool-poisoning-scanner.html",
    "function_name": "runScan",
    "timeout_ms": 3000
  },
  "ap2_export": true,
  "prefill": true,
  "bridge_version": "1.0"
}
