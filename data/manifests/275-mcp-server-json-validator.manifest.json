{
  "tool_id": "mcp-server-json-validator",
  "version": "1.1.0",
  "title": "MCP server.json Validator & Registry-Ready Skeleton Generator",
  "description": "Validates an MCP server.json against the 2025-12-11 generic schema and the official MCP Registry publishing constraints (reverse-DNS namespace, _meta 4KB publisher cap, allowlisted registry base URLs, MCPB fileSha256, stdio/streamable-http transport), and generates a compliant skeleton for npm/PyPI/OCI/MCPB/remote servers. Reference rules are static and may age — verify against the live MCP Registry docs.",
  "category": "cat-1",
  "tags": [
    "MCP",
    "server.json",
    "registry",
    "schema validation",
    "publishing",
    "agent tooling"
  ],
  "audience": [
    "engineer",
    "product"
  ],
  "input_schema": {
    "type": "object",
    "properties": {
      "server_json": {
        "type": "object",
        "description": "An MCP server.json document with name, description, version, and packages or remotes."
      }
    },
    "required": [
      "server_json"
    ]
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "score": {
        "type": "number"
      },
      "findings": {
        "type": "array",
        "items": {
          "type": "object"
        }
      },
      "skeleton": {
        "type": "object"
      }
    }
  },
  "mcp_tool_definition": {
    "name": "validate_mcp_server_json",
    "description": "Validate an MCP server.json against the 2025-12-11 schema and the official registry publishing rules; returns findings, a registry-readiness score, and an optional compliant skeleton. Use when a developer wants to check a server.json before publishing to the MCP Registry.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "server_json": {
          "type": "object",
          "description": "The MCP server.json document to validate."
        }
      },
      "required": [
        "server_json"
      ]
    }
  },
  "execution": {
    "type": "browser-javascript",
    "entry": "tools/275-mcp-server-json-validator.html",
    "function_name": "runValidator",
    "timeout_ms": 3000
  },
  "ap2_export": true,
  "prefill": true,
  "bridge_version": "1.0"
}
