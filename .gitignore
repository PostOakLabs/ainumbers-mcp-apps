node_modules/
nohup.out
.dev.vars

# MCP registry publishing — never commit key material or the CLI binary
key.pem
pub.der
mcp-registry-auth
mcp-publisher.exe
mcp-publisher.tar.gz

bundled/
.wrangler/
